language: node_js
node_js:
  - "stable"
install:
  - npm -g install yarn
  - yarn
cache:
  directories:
    - ~/.yarn
    - ~/.nvm
script: npm run travis
after_success:
  - 'cat ./coverage/lcov.info | ./node_modules/.bin/coveralls'
